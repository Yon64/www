////////// Variables globales et Initialisation  //////////

function Init()
{
/*	
	const element = document.getElementById('choix');
	if (element != null)
	{
		const choices = new Choices(element, {
				removeItemButton: true,  // Ajoute un bouton pour supprimer les éléments sélectionnés
				shouldSort: false,
	//			maxItemCount: 5,         // Limite le nombre d'éléments sélectionnés à 5
	//			searchResultLimit: 5,    // Limite le nombre de résultats de recherche affichés
	//			renderChoiceLimit: 5     // Limite le nombre d'options rendues
		});
	}
*/
}
